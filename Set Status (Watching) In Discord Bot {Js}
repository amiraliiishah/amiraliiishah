// Set the bot's presence (activity and status)
client.on("ready", () => {
    client.user.setPresence({
        activities: [{ 
          name: "35 Members",
          type: "WATCHING"
        }],
        status: "online"
    })
})
